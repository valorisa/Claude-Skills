# Skills Smart Manager — Session Report

**Generated:** {{timestamp}}  
**Context Status:** {{context_percentage}}% full  
**Total Skill Tokens:** {{total_skill_tokens}} tokens

---

## Current Session Analysis

### Active Skills

| Skill | Tokens | Last Used | Status |
|-------|--------|-----------|--------|
| skill-name | 3200 | 2 turns ago | active |
| another-skill | 2800 | 0 turns ago | active |

---

## Recommendations

### ⚠️ Unload Candidates

**skill-name** — Not used in 45 turns  
Reason: Stale, domain mismatch  
**Savings:** ~2400 tokens

### ✅ Keep Active

**tdd-hybrid** — Active (last used 2 turns ago)  
**rescue-tokens** — Active (last used 18 turns ago, but relevant to context pressure)

---

## Project Analysis

**Detected Project Type:** {{project_type}}

### Missing Recommended Skills

- **rust-best-practices** — Cargo.toml detected
- **cargo-optimizer** — Recommended for Rust projects

Or if none missing:

✅ All recommended skills for rust-cargo are already installed.

---

## Conflict Detection

### Example: Conflicts Found

⚠️ **Conflicts Found:**

#### prettier-formatter vs standard-js-linter

**Issue:** Overlapping formatting rules

**Recommended Action:** Keep prettier-formatter, unload standard-js-linter

### Or if no conflicts

✅ No conflicts detected between active skills.

---

## Potential Impact

### If Recommendations Applied

- **Tokens Saved:** {{total_tokens_saved}} tokens ({{percentage_reduction}}% reduction)
- **Context Freed:** {{context_percentage_freed}}%
- **New Context Level:** {{new_context_percentage}}%

### Session Performance

- **Expected Latency Improvement:** {{latency_improvement}}
- **Messages Until Rate Limit:** {{messages_remaining}} (before) → {{messages_after}} (after)

---

## Actions

**Apply these optimizations?**

```
python3 scripts/analyze_context.py --action unload --skills {{unload_list}}
```

Or respond:

- ✅ **"Apply all"** — Unload all recommended skills
- 🔧 **"Apply selective"** — Choose which to unload
- ❌ **"Cancel"** — Keep current configuration

---

## Health Check Results

| Component | Status | Notes |
|-----------|--------|-------|
| GitHub MCP | ✅ ok | Responding (72ms) |
| OpenAI API | ❌ error | Key not found in environment |
| gh CLI | ✅ ok | Installed (v2.40.1) |
| docker | ⚠️ warning | Installed but daemon not running |

Or if not performed:

_Health check not performed. Run with `--action health-check` to verify dependencies._

---

## Session Memory

### If memory exists

✅ Session memory file exists: `.skill-memory.json`

This repository has saved skill preferences from previous sessions.

### If no memory

💾 **Create session memory?**

Save optimal skill configuration for this repository:

```bash
python3 scripts/analyze_context.py --action archive
```

Next time you work on this project, recommended skills will auto-load.

---

## Temporary Files

### If temp files exist

🗑️ **Temporary files detected:**

- `.claude-export-report.pdf` (128 KB)
- `diagnose-session-42.log` (64 KB)

**Clean up?**

```bash
python3 scripts/analyze_context.py --action gc
```

### If no temp files

✅ No temporary skill files detected.

---

## Notes

- Staleness threshold: {{staleness_threshold}} turns
- Context threshold: {{context_threshold}}%
- Auto-unload: {{auto_unload_enabled}}

**Adjust settings:**

```bash
# Change staleness threshold
python3 scripts/analyze_context.py --action scan --staleness-threshold 30

# Enable automatic unloading (use with caution)
python3 scripts/analyze_context.py --action scan --auto-unload
```

---

## Next Steps

1. **Review recommendations** above
2. **Apply optimizations** if approved
3. **Re-run scan** to verify improvements
4. **Archive session memory** for future sessions

**Questions?**

- See `SKILL.md` for full documentation
- Check `references/conflict-rules.md` for conflict resolution logic
- Run `python3 scripts/analyze_context.py --help` for CLI options

---

_Generated by Skills Smart Manager v1.0.0_
